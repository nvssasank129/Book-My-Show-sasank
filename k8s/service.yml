apiVersion: v1
kind: Service
metadata:
  name: bms-service
  namespace: sasank-ns
spec:
  type: LoadBalancer
  selector:
    app: bms
  ports:
  - protocol: TCP
    port: 3090        # External port exposed to users
    targetPort: 3090  # Internal port your app listens on